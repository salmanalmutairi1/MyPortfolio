import React from 'react';

/**
 * Brutalist redesign: the violet particle canvas was retired in favor of
 * fixed grain + grid composition. This component is intentionally a no-op
 * to avoid touching every consumer.
 */
const Particles: React.FC = () => null;

export default Particles;
